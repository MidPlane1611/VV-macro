-- Загрузка настроек
local success, Settings = pcall(function()
    return loadstring(readfile("settings.lua"))()
end)

if not success or not Settings then
    Settings = { 
        Theme = "Dark", 
        FPS = 60, 
        BlackScreen = false,
        Webhook = "",
        SprinklerType = "Diamond Sprinkler",
        SelectedField = "Pepper (35 bee)",
        FarmType = "Random",
        ConvertMode = "At 100% Bag"
    }
end

local currentTheme = {
    Background = Color3.fromRGB(24, 24, 27),
    Header = Color3.fromRGB(18, 18, 20),
    Secondary = Color3.fromRGB(35, 35, 39),
    Text = Color3.fromRGB(240, 240, 245),
    SubText = Color3.fromRGB(160, 160, 170),
    Accent = Color3.fromRGB(59, 130, 246)
}

local CoreGui = game:GetService("CoreGui")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DeathNoteVGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 2147483647
ScreenGui.Parent = CoreGui

-- Мобильная плавающая кнопка (исправлено: добавлен Parent и ZIndex)
local IconBtn = Instance.new("TextButton")
IconBtn.Size = UDim2.new(0, 50, 0, 50)
IconBtn.Position = UDim2.new(0, 100, 0, 150)
IconBtn.BackgroundColor3 = currentTheme.Background
IconBtn.BorderSizePixel = 0
IconBtn.Text = ""
IconBtn.Active = true
IconBtn.AutoButtonColor = false
IconBtn.ZIndex = 9999
IconBtn.Parent = ScreenGui

local IconCorner = Instance.new("UICorner")
IconCorner.CornerRadius = UDim.new(0, 10)
IconCorner.Parent = IconBtn

local LogoImage = Instance.new("ImageLabel")
LogoImage.Size = UDim2.new(0, 40, 0, 40)
LogoImage.Position = UDim2.new(0, 5, 0, 5)
LogoImage.BackgroundTransparency = 1
LogoImage.Image = "rbxassetid://112576205931289"
LogoImage.ZIndex = 10000
LogoImage.Parent = IconBtn

-- Главное окно меню
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 580, 0, 380)
MainFrame.Position = UDim2.new(0.5, -290, 0.5, -190)
MainFrame.BackgroundColor3 = currentTheme.Background
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.ZIndex = 5000
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundColor3 = currentTheme.Header
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 8)
TopBarCorner.Parent = TopBar

local FixCorner = Instance.new("Frame")
FixCorner.Size = UDim2.new(1, 0, 0, 5)
FixCorner.Position = UDim2.new(0, 0, 1, -5)
FixCorner.BackgroundColor3 = currentTheme.Header
FixCorner.BorderSizePixel = 0
FixCorner.Parent = TopBar

local MainTabsContainer = Instance.new("ScrollingFrame")
MainTabsContainer.Size = UDim2.new(1, -60, 1, 0)
MainTabsContainer.Position = UDim2.new(0, 15, 0, 0)
MainTabsContainer.BackgroundTransparency = 1
MainTabsContainer.CanvasSize = UDim2.new(0, 400, 0, 0)
MainTabsContainer.ScrollBarThickness = 0
MainTabsContainer.Parent = TopBar

local MainTabsLayout = Instance.new("UIListLayout")
MainTabsLayout.FillDirection = Enum.FillDirection.Horizontal
MainTabsLayout.SortOrder = Enum.SortOrder.LayoutOrder
MainTabsLayout.Padding = UDim.new(0, 20)
MainTabsLayout.Parent = MainTabsContainer

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -35, 0, 5)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = currentTheme.SubText
CloseBtn.TextSize = 16
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.Parent = TopBar
CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    IconBtn.Visible = true
end)

local ContentArea = Instance.new("Frame")
ContentArea.Size = UDim2.new(1, 0, 1, -40)
ContentArea.Position = UDim2.new(0, 0, 0, 40)
ContentArea.BackgroundTransparency = 1
ContentArea.Parent = MainFrame

local mainPages = {}
local function createMainPage(name)
    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1, -20, 1, -20)
    page.Position = UDim2.new(0, 10, 0, 10)
    page.BackgroundTransparency = 1
    page.CanvasSize = UDim2.new(0, 0, 0, 350)
    page.ScrollBarThickness = 3
    page.Visible = false
    page.Parent = ContentArea

    local layout = Instance.new("UIListLayout")
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 8)
    layout.Parent = page

    mainPages[name] = page
    return page
end

local pageGather = createMainPage("Gather")
local pageKillMobs = createMainPage("Kill Mobs")
local pageKillBoss = createMainPage("Kill Boss")
local pageSettings = createMainPage("Settings")

local function switchMainPage(name)
    for pName, pObj in pairs(mainPages) do
        pObj.Visible = (pName == name)
    end
end
switchMainPage("Gather")

local function createMainTabButton(name, width)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, width or 75, 1, 0)
    btn.BackgroundTransparency = 1
    btn.Text = name
    btn.TextColor3 = currentTheme.SubText
    btn.TextSize = 13
    btn.Font = Enum.Font.SourceSansSemibold
    btn.Parent = MainTabsContainer

    btn.MouseButton1Click:Connect(function()
        switchMainPage(name)
    end)
end

createMainTabButton("Gather", 55)
createMainTabButton("Kill Mobs", 70)
createMainTabButton("Kill Boss", 65)
createMainTabButton("Settings", 60)

local function createSelectorRow(parent, labelName, optionsList, defaultIdx, callback)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, 0, 0, 40)
    Row.BackgroundColor3 = currentTheme.Secondary
    Row.BorderSizePixel = 0
    Row.Parent = parent

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = Row

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0, 160, 1, 0)
    Label.Position = UDim2.new(0, 14, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Text = labelName
    Label.TextColor3 = currentTheme.Text
    Label.TextSize = 13
    Label.Font = Enum.Font.SourceSansSemibold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Row

    local currentIndex = defaultIdx
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(0, 150, 0, 26)
    Btn.Position = UDim2.new(1, -160, 0.5, -13)
    Btn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
    Btn.Text = optionsList[currentIndex]
    Btn.TextColor3 = currentTheme.Text
    Btn.TextSize = 12
    Btn.Font = Enum.Font.SourceSansBold
    Btn.Parent = Row

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = Btn

    Btn.MouseButton1Click:Connect(function()
        currentIndex = currentIndex % #optionsList + 1
        Btn.Text = optionsList[currentIndex]
        callback(optionsList[currentIndex])
    end)
end

local fieldsList = {
    "Dandelion (1 bee)", "Mushroom (1 bee)", "Sunflower (1 bee)", "Blue flower (1 bee)", "Clover (1 bee)",
    "Spider (5 bee)", "Strawberry (5 bee)", "Bamboo (5 bee)", "Pineapple (10 bee)", "Stump (10 bee)",
    "Cactus (15 bee)", "Pumpkin (15 bee)", "Pine tree (15 bee)", "Rose (15 bee)", "Mountain top (25 bee)",
    "Coconut (35 bee)", "Pepper (35 bee)"
}

local function createRayfieldDropdown(parent, labelName, items, defaultVal, callback)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, 0, 0, 40)
    Row.BackgroundColor3 = currentTheme.Secondary
    Row.BorderSizePixel = 0
    Row.ZIndex = 2
    Row.Parent = parent

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = Row

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0, 160, 1, 0)
    Label.Position = UDim2.new(0, 14, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Text = labelName
    Label.TextColor3 = currentTheme.Text
    Label.TextSize = 13
    Label.Font = Enum.Font.SourceSansSemibold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.ZIndex = 2
    Label.Parent = Row

    local MainBtn = Instance.new("TextButton")
    MainBtn.Size = UDim2.new(0, 180, 0, 26)
    MainBtn.Position = UDim2.new(1, -190, 0.5, -13)
    MainBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
    MainBtn.Text = defaultVal
    MainBtn.TextColor3 = currentTheme.Text
    MainBtn.TextSize = 12
    MainBtn.Font = Enum.Font.SourceSansBold
    MainBtn.ZIndex = 2
    MainBtn.Parent = Row

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = MainBtn

    local DropList = Instance.new("ScrollingFrame")
    DropList.Size = UDim2.new(0, 180, 0, 140)
    DropList.Position = UDim2.new(1, -190, 1, 4)
    DropList.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    DropList.BorderSizePixel = 0
    DropList.CanvasSize = UDim2.new(0, 0, 0, #items * 30)
    DropList.ScrollBarThickness = 4
    DropList.Visible = false
    DropList.ZIndex = 10
    DropList.Parent = Row

    local DropCorner = Instance.new("UICorner")
    DropCorner.CornerRadius = UDim.new(0, 6)
    DropCorner.Parent = DropList

    local DropLayout = Instance.new("UIListLayout")
    DropLayout.SortOrder = Enum.SortOrder.LayoutOrder
    DropLayout.Parent = DropList

    for _, itemText in ipairs(items) do
        local OptBtn = Instance.new("TextButton")
        OptBtn.Size = UDim2.new(1, 0, 0, 30)
        OptBtn.BackgroundTransparency = 1
        OptBtn.Text = itemText
        OptBtn.TextColor3 = currentTheme.SubText
        OptBtn.TextSize = 12
        OptBtn.Font = Enum.Font.SourceSans
        OptBtn.ZIndex = 10
        OptBtn.Parent = DropList

        OptBtn.MouseButton1Click:Connect(function()
            MainBtn.Text = itemText
            DropList.Visible = false
            callback(itemText)
        end)
    end

    MainBtn.MouseButton1Click:Connect(function()
        DropList.Visible = not DropList.Visible
    end)
end

createRayfieldDropdown(pageGather, "Select Field", fieldsList, Settings.SelectedField, function(val)
    Settings.SelectedField = val
end)

createSelectorRow(pageGather, "Farm Type", {"Random"}, 1, function(val)
    Settings.FarmType = val
end)

createSelectorRow(pageGather, "Sprinkler Type", {"Basic Sprinkler", "Silver Sprinkler", "Golden Sprinkler", "Diamond Sprinkler", "Supreme Sprinkler"}, 4, function(val)
    Settings.SprinklerType = val
end)

createSelectorRow(pageGather, "Pollen Conversion", {"At 100% Bag", "At 80% Bag", "Every 5 Minutes", "Manual Only"}, 1, function(val)
    Settings.ConvertMode = val
end)

local mobsData = {
    {Name = "Ladybug", Fields = {"Mushroom", "Strawberry", "Clover"}},
    {Name = "Rhino Beetle", Fields = {"Clover", "Blue Flower", "Bamboo", "Pineapple"}},
    {Name = "Spider", Fields = {"Spider"}},
    {Name = "Mantis", Fields = {"Pineapple", "Pine Tree"}},
    {Name = "Scorpion", Fields = {"Rose"}},
    {Name = "Werewolf", Fields = {"Cactus"}}
}

local MobsSubTabsBar = Instance.new("ScrollingFrame")
MobsSubTabsBar.Size = UDim2.new(1, 0, 0, 30)
MobsSubTabsBar.BackgroundTransparency = 1
MobsSubTabsBar.CanvasSize = UDim2.new(0, 450, 0, 0)
MobsSubTabsBar.ScrollBarThickness = 0
MobsSubTabsBar.Parent = pageKillMobs

local MobsSubLayout = Instance.new("UIListLayout")
MobsSubLayout.FillDirection = Enum.FillDirection.Horizontal
MobsSubLayout.SortOrder = Enum.SortOrder.LayoutOrder
MobsSubLayout.Padding = UDim.new(0, 6)
MobsSubLayout.Parent = MobsSubTabsBar

local MobsContentContainer = Instance.new("Frame")
MobsContentContainer.Size = UDim2.new(1, 0, 1, -38)
MobsContentContainer.BackgroundTransparency = 1
MobsContentContainer.Parent = pageKillMobs

local mobPages = {}

for _, mob in ipairs(mobsData) do
    local SubBtn = Instance.new("TextButton")
    SubBtn.Size = UDim2.new(0, 85, 1, 0)
    SubBtn.BackgroundColor3 = currentTheme.Secondary
    SubBtn.Text = mob.Name
    SubBtn.TextColor3 = currentTheme.Text
    SubBtn.TextSize = 11
    SubBtn.Font = Enum.Font.SourceSansSemibold
    SubBtn.Parent = MobsSubTabsBar

    local SubCorner = Instance.new("UICorner")
    SubCorner.CornerRadius = UDim.new(0, 6)
    SubCorner.Parent = SubBtn

    local MobPage = Instance.new("ScrollingFrame")
    MobPage.Size = UDim2.new(1, 0, 1, 0)
    MobPage.Position = UDim2.new(0, 0, 0, 8)
    MobPage.BackgroundTransparency = 1
    MobPage.CanvasSize = UDim2.new(0, 0, 0, 300)
    MobPage.ScrollBarThickness = 3
    MobPage.Visible = false
    MobPage.Parent = MobsContentContainer

    local MobPageLayout = Instance.new("UIListLayout")
    MobPageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    MobPageLayout.Padding = UDim.new(0, 8)
    MobPageLayout.Parent = MobPage

    mobPages[mob.Name] = MobPage

    SubBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(mobPages) do p.Visible = false end
        MobPage.Visible = true
    end)

    local KillRow = Instance.new("Frame")
    KillRow.Size = UDim2.new(1, 0, 0, 40)
    KillRow.BackgroundColor3 = currentTheme.Secondary
    KillRow.BorderSizePixel = 0
    KillRow.Parent = MobPage

    local KCorner = Instance.new("UICorner")
    KCorner.CornerRadius = UDim.new(0, 6)
    KCorner.Parent = KillRow

    local KLabel = Instance.new("TextLabel")
    KLabel.Size = UDim2.new(1, -60, 1, 0)
    KLabel.Position = UDim2.new(0, 14, 0, 0)
    KLabel.BackgroundTransparency = 1
    KLabel.Text = "Kill " .. mob.Name
    KLabel.TextColor3 = currentTheme.Text
    KLabel.TextSize = 13
    KLabel.Font = Enum.Font.SourceSansSemibold
    KLabel.TextXAlignment = Enum.TextXAlignment.Left
    KLabel.Parent = KillRow

    local KToggle = Instance.new("TextButton")
    KToggle.Size = UDim2.new(0, 40, 0, 24)
    KToggle.Position = UDim2.new(1, -50, 0.5, -12)
    KToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 65)
    KToggle.Text = ""
    KToggle.Parent = KillRow

    local KToggleCorner = Instance.new("UICorner")
    KToggleCorner.CornerRadius = UDim.new(1, 0)
    KToggleCorner.Parent = KToggle

    local kState = false
    KToggle.MouseButton1Click:Connect(function()
        kState = not kState
        KToggle.BackgroundColor3 = kState and currentTheme.Accent or Color3.fromRGB(60, 60, 65)
        Settings["Kill" .. mob.Name] = kState
    end)

    for _, fieldName in ipairs(mob.Fields) do
        local FRow = Instance.new("Frame")
        FRow.Size = UDim2.new(1, 0, 0, 40)
        FRow.BackgroundColor3 = currentTheme.Secondary
        FRow.BorderSizePixel = 0
        FRow.Parent = MobPage

        local FCorner = Instance.new("UICorner")
        FCorner.CornerRadius = UDim.new(0, 6)
        FCorner.Parent = FRow

        local FLabel = Instance.new("TextLabel")
        FLabel.Size = UDim2.new(1, -60, 1, 0)
        FLabel.Position = UDim2.new(0, 14, 0, 0)
        FLabel.BackgroundTransparency = 1
        FLabel.Text = fieldName
        FLabel.TextColor3 = currentTheme.Text
        FLabel.TextSize = 13
        FLabel.Font = Enum.Font.SourceSansSemibold
        FLabel.TextXAlignment = Enum.TextXAlignment.Left
        FLabel.Parent = FRow

        local FToggle = Instance.new("TextButton")
        FToggle.Size = UDim2.new(0, 40, 0, 24)
        FToggle.Position = UDim2.new(1, -50, 0.5, -12)
        FToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 65)
        FToggle.Text = ""
        FToggle.Parent = FRow

        local FToggleCorner = Instance.new("UICorner")
        FToggleCorner.CornerRadius = UDim.new(1, 0)
        FToggleCorner.Parent = FToggle

        local fState = false
        FToggle.MouseButton1Click:Connect(function()
            fState = not fState
            FToggle.BackgroundColor3 = fState and currentTheme.Accent or Color3.fromRGB(60, 60, 65)
            Settings[mob.Name .. "_" .. fieldName] = fState
        end)
    end

    createSelectorRow(MobPage, "Priority (1-6)", {"1", "2", "3", "4", "5", "6"}, 6, function(val)
        Settings[mob.Name .. "_Priority"] = tonumber(val)
    end)
end

mobPages["Ladybug"].Visible = true

local bossData = {"King Beetle", "Mondo Chick", "Tunnel Bear", "Snail"}

local BossSubTabsBar = Instance.new("ScrollingFrame")
BossSubTabsBar.Size = UDim2.new(1, 0, 0, 30)
BossSubTabsBar.BackgroundTransparency = 1
BossSubTabsBar.CanvasSize = UDim2.new(0, 400, 0, 0)
BossSubTabsBar.ScrollBarThickness = 0
BossSubTabsBar.Parent = pageKillBoss

local BossSubLayout = Instance.new("UIListLayout")
BossSubLayout.FillDirection = Enum.FillDirection.Horizontal
BossSubLayout.SortOrder = Enum.SortOrder.LayoutOrder
BossSubLayout.Padding = UDim.new(0, 6)
BossSubLayout.Parent = BossSubTabsBar

local BossContentContainer = Instance.new("Frame")
BossContentContainer.Size = UDim2.new(1, 0, 1, -38)
BossContentContainer.BackgroundTransparency = 1
BossContentContainer.Parent = pageKillBoss

local bossPages = {}

for _, bossName in ipairs(bossData) do
    local SubBtn = Instance.new("TextButton")
    SubBtn.Size = UDim2.new(0, 95, 1, 0)
    SubBtn.BackgroundColor3 = currentTheme.Secondary
    SubBtn.Text = bossName
    SubBtn.TextColor3 = currentTheme.Text
    SubBtn.TextSize = 11
    SubBtn.Font = Enum.Font.SourceSansSemibold
    SubBtn.Parent = BossSubTabsBar

    local SubCorner = Instance.new("UICorner")
    SubCorner.CornerRadius = UDim.new(0, 6)
    SubCorner.Parent = SubBtn

    local BossPage = Instance.new("ScrollingFrame")
    BossPage.Size = UDim2.new(1, 0, 1, 0)
    BossPage.Position = UDim2.new(0, 0, 0, 8)
    BossPage.BackgroundTransparency = 1
    BossPage.CanvasSize = UDim2.new(0, 0, 0, 100)
    BossPage.ScrollBarThickness = 3
    BossPage.Visible = false
    BossPage.Parent = BossContentContainer

    local BossPageLayout = Instance.new("UIListLayout")
    BossPageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    BossPageLayout.Padding = UDim.new(0, 8)
    BossPageLayout.Parent = BossPage

    bossPages[bossName] = BossPage

    SubBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(bossPages) do p.Visible = false end
        BossPage.Visible = true
    end)

    local KillRow = Instance.new("Frame")
    KillRow.Size = UDim2.new(1, 0, 0, 40)
    KillRow.BackgroundColor3 = currentTheme.Secondary
    KillRow.BorderSizePixel = 0
    KillRow.Parent = BossPage

    local KCorner = Instance.new("UICorner")
    KCorner.CornerRadius = UDim.new(0, 6)
    KCorner.Parent = KillRow

    local KLabel = Instance.new("TextLabel")
    KLabel.Size = UDim2.new(1, -60, 1, 0)
    KLabel.Position = UDim2.new(0, 14, 0, 0)
    KLabel.BackgroundTransparency = 1
    KLabel.Text = "Kill " .. bossName
    KLabel.TextColor3 = currentTheme.Text
    KLabel.TextSize = 13
    KLabel.Font = Enum.Font.SourceSansSemibold
    KLabel.TextXAlignment = Enum.TextXAlignment.Left
    KLabel.Parent = KillRow

    local KToggle = Instance.new("TextButton")
    KToggle.Size = UDim2.new(0, 40, 0, 24)
    KToggle.Position = UDim2.new(1, -50, 0.5, -12)
    KToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 65)
    KToggle.Text = ""
    KToggle.Parent = KillRow

    local KToggleCorner = Instance.new("UICorner")
    KToggleCorner.CornerRadius = UDim.new(1, 0)
    KToggleCorner.Parent = KToggle

    local kState = false
    KToggle.MouseButton1Click:Connect(function()
        kState = not kState
        KToggle.BackgroundColor3 = kState and currentTheme.Accent or Color3.fromRGB(60, 60, 65)
        Settings["Kill" .. bossName] = kState
    end)
end

bossPages["King Beetle"].Visible = true

createSelectorRow(pageSettings, "FPS", {"30 FPS", "60 FPS", "120 FPS", "Unlimited"}, 2, function(val)
    Settings.FPS = val
end)

createRayfieldDropdown(pageSettings, "Theme", {"Dark", "Light", "Lime"}, "Dark", function(val)
    Settings.Theme = val
end)

local function createToggleRow(parent, name, defaultState, callback)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, 0, 0, 40)
    Row.BackgroundColor3 = currentTheme.Secondary
    Row.BorderSizePixel = 0
    Row.Parent = parent

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = Row

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(1, -60, 1, 0)
    Label.Position = UDim2.new(0, 14, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Text = name
    Label.TextColor3 = currentTheme.Text
    Label.TextSize = 13
    Label.Font = Enum.Font.SourceSansSemibold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Row

    local ToggleBtn = Instance.new("TextButton")
    ToggleBtn.Size = UDim2.new(0, 40, 0, 24)
    ToggleBtn.Position = UDim2.new(1, -50, 0.5, -12)
    ToggleBtn.BackgroundColor3 = defaultState and currentTheme.Accent or Color3.fromRGB(60, 60, 65)
    ToggleBtn.Text = ""
    ToggleBtn.Parent = Row

    local ToggleCorner = Instance.new("UICorner")
    ToggleCorner.CornerRadius = UDim.new(1, 0)
    ToggleCorner.Parent = ToggleBtn

    local state = defaultState
    ToggleBtn.MouseButton1Click:Connect(function()
        state = not state
        ToggleBtn.BackgroundColor3 = state and currentTheme.Accent or Color3.fromRGB(60, 60, 65)
        callback(state)
    end)
end

createToggleRow(pageSettings, "Black Screen", false, function(state)
    Settings.BlackScreen = state
end)

local WebhookRow = Instance.new("Frame")
WebhookRow.Size = UDim2.new(1, 0, 0, 40)
WebhookRow.BackgroundColor3 = currentTheme.Secondary
WebhookRow.BorderSizePixel = 0
WebhookRow.Parent = pageSettings

local WHCorner = Instance.new("UICorner")
WHCorner.CornerRadius = UDim.new(0, 6)
WHCorner.Parent = WebhookRow

local WHLabel = Instance.new("TextLabel")
WHLabel.Size = UDim2.new(0, 120, 1, 0)
WHLabel.Position = UDim2.new(0, 14, 0, 0)
WHLabel.BackgroundTransparency = 1
WHLabel.Text = "Webhook"
WHLabel.TextColor3 = currentTheme.Text
WHLabel.TextSize = 13
WHLabel.Font = Enum.Font.SourceSansSemibold
WHLabel.TextXAlignment = Enum.TextXAlignment.Left
WHLabel.Parent = WebhookRow

local TextBox = Instance.new("TextBox")
TextBox.Size = UDim2.new(0, 150, 0, 26)
TextBox.Position = UDim2.new(1, -160, 0.5, -13)
TextBox.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
TextBox.PlaceholderText = "Enter Webhook..."
TextBox.Text = Settings.Webhook
TextBox.TextColor3 = currentTheme.Text
TextBox.TextSize = 11
TextBox.Font = Enum.Font.SourceSans
TextBox.Parent = WebhookRow

local BoxCorner = Instance.new("UICorner")
BoxCorner.CornerRadius = UDim.new(0, 6)
BoxCorner.Parent = TextBox

TextBox.FocusLost:Connect(function()
    Settings.Webhook = TextBox.Text
end)

-- Открытие и закрытие главного меню по клику/тапу на иконку
local isOpen = false
IconBtn.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    MainFrame.Visible = isOpen
end)

IconBtn.TouchTap:Connect(function()
    isOpen = not isOpen
    MainFrame.Visible = isOpen
end)
